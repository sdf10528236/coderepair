#include <stdio.h>
#include <stdlib.h>

int main()
{
    
    
	
	int score ;
	char grade;
	scanf("%d%c", &score, &grade);
	if(score>=90){
    printf("%d %c", score, grade);
	}else if(score>=80){
    printf("%d%c", score, grade)
    }else if(score>=70){
    printf("%d%c", score, grade)
    }else if(score>=60){
    printf("%d%c", score, grade)
	}else ;
    printf("%d%c", score, grade)			
	return 0; 
		
    
}
